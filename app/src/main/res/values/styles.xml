<resources>
    <!-- Base application theme. -->
    <style name="Theme.LoginRecu" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <!-- Estilo para los botones de inicio de sesión -->
    <style name="LoginButtonStyle" parent="Widget.AppCompat.Button.Colored">
        <item name="android:backgroundTint">@color/dark_green</item>
        <item name="android:textColor">@color/white</item>
    </style>
</resources>
